/* =====================================================================
   app-init.js — אתחול האפליקציה לאחר טעינת כל הסקריפטים
   ===================================================================== */
setupNavigation();
addCourse();              // שני קורסים כברירת מחדל = מינימום דאטת אימון
addCourse();
showAlgoSummary();
buildAllVisualizations();
